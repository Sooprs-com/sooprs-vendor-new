diff --git a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/Constants.java b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/Constants.java
index ef216a4..fd0d930 100644
--- a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/Constants.java
+++ b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/Constants.java
@@ -15,5 +15,12 @@ public class Constants {
   public static final String RNNotificationEndCallAction="RNNotificationEndCallAction";
   public static final String onPressNotification="onPressNotification";
 
+  public static final String ACTION_INCOMING_CALL_ANSWERED =
+    "com.sooprsvendor.action.INCOMING_CALL_ANSWERED";
+  public static final String ACTION_INCOMING_CALL_DECLINED =
+    "com.sooprsvendor.action.INCOMING_CALL_DECLINED";
+  public static final String ACTION_STOP_INCOMING_RINGTONE =
+    "com.sooprsvendor.action.STOP_INCOMING_RINGTONE";
+  
 
 }
diff --git a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/FullScreenNotificationIncomingCallModule.java b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/FullScreenNotificationIncomingCallModule.java
index 7f6e7af..0849384 100644
--- a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/FullScreenNotificationIncomingCallModule.java
+++ b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/FullScreenNotificationIncomingCallModule.java
@@ -4,7 +4,6 @@ import android.app.Activity;
 import android.content.Context;
 import android.content.Intent;
 import android.util.Log;
-import android.view.WindowManager;
 
 import androidx.annotation.NonNull;
 import androidx.annotation.Nullable;
@@ -89,6 +88,9 @@ public class FullScreenNotificationIncomingCallModule extends ReactContextBaseJa
   }
 
   public static void sendEventToJs(String eventName, @Nullable WritableMap params) {
+    if (reactContext == null || !reactContext.hasActiveReactInstance()) {
+      return;
+    }
     reactContext.getJSModule(DeviceEventManagerModule.RCTDeviceEventEmitter.class).emit(eventName, params);
   }
 
@@ -109,15 +111,25 @@ public class FullScreenNotificationIncomingCallModule extends ReactContextBaseJa
     String packageName = context.getApplicationContext().getPackageName();
     Intent focusIntent = context.getPackageManager().getLaunchIntentForPackage(packageName).cloneFilter();
     Activity activity = getCurrentReactActivity();
-    boolean isOpened = activity != null;
+    // The "current" activity here is frequently the full-screen incoming-call
+    // Activity itself, which is usually mid-teardown (finishAndRemoveTask()
+    // was just requested by hideNotification()/answerCall()). If we only add
+    // FLAG_ACTIVITY_REORDER_TO_FRONT (no FLAG_ACTIVITY_NEW_TASK) and launch
+    // from that dying activity, the target (main) Activity can be attached to
+    // the same task that is being removed, so it gets torn down together with
+    // it a moment later. Always requesting a NEW_TASK guarantees the main
+    // Activity gets its own independent task regardless of the caller's task.
+    boolean isOpened = activity != null && !activity.isFinishing() && !activity.isDestroyed();
+    focusIntent.addFlags(
+      Intent.FLAG_ACTIVITY_NEW_TASK |
+      Intent.FLAG_ACTIVITY_CLEAR_TOP |
+      Intent.FLAG_ACTIVITY_SINGLE_TOP |
+      Intent.FLAG_ACTIVITY_REORDER_TO_FRONT
+    );
+    focusIntent.putExtra("from_incoming_call", true);
     if (!isOpened) {
-      focusIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK +
-          WindowManager.LayoutParams.FLAG_SHOW_WHEN_LOCKED +
-          WindowManager.LayoutParams.FLAG_DISMISS_KEYGUARD +
-          WindowManager.LayoutParams.FLAG_TURN_SCREEN_ON);
       getReactApplicationContext().startActivity(focusIntent);
     } else {
-      focusIntent.addFlags(Intent.FLAG_ACTIVITY_REORDER_TO_FRONT);
       activity.startActivity(focusIntent);
     }
   }
diff --git a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallActivity.java b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallActivity.java
index 3395f00..531bbda 100644
--- a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallActivity.java
+++ b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallActivity.java
@@ -93,10 +93,20 @@ public class IncomingCallActivity extends AppCompatActivity implements DefaultHa
     if (bundle.containsKey("mainComponent") && bundle.getString("mainComponent") != null) {
       String mainComponent = bundle.getString("mainComponent");
       setContentView(R.layout.custom_ingcoming_call_rn);
-      Fragment reactNativeFragment = new ReactFragment.Builder()
-        .setComponentName(mainComponent)
-        .setLaunchOptions(bundle)
-        .build();
+      ReactFragment.Builder builder = new ReactFragment.Builder();
+      try {
+        // RN 0.82 exposes both Kotlin property setters and fluent setters with
+        // these Java signatures. Reflection avoids javac's ambiguous call.
+        builder.getClass()
+          .getMethod("setComponentName", String.class)
+          .invoke(builder, mainComponent);
+        builder.getClass()
+          .getMethod("setLaunchOptions", Bundle.class)
+          .invoke(builder, bundle);
+      } catch (ReflectiveOperationException exception) {
+        throw new RuntimeException("Unable to configure ReactFragment", exception);
+      }
+      Fragment reactNativeFragment = builder.build();
 
       getSupportFragmentManager()
         .beginTransaction()
diff --git a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallService.java b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallService.java
index 502fe36..427dad9 100644
--- a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallService.java
+++ b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/IncomingCallService.java
@@ -340,6 +340,7 @@ public class IncomingCallService extends Service {
         params.putString("callUUID", uuid);
         params.putString("endAction", Constants.ACTION_HIDE_CALL);
         FullScreenNotificationIncomingCallModule.sendEventToJs(Constants.RNNotificationEndCallAction, params);
+        sendAppBroadcast(Constants.ACTION_STOP_INCOMING_RINGTONE, bundleData);
         cancelTimer();
         stopForeground(true);
       }
@@ -372,6 +373,15 @@ public class IncomingCallService extends Service {
     return desiredColor;
   }
 
+  private void sendAppBroadcast(String action, Bundle bundle) {
+    Intent intent = new Intent(action);
+    intent.setPackage(getPackageName());
+    if (bundle != null) {
+      intent.putExtras(bundle);
+    }
+    sendBroadcast(intent);
+  }
+
   private BroadcastReceiver mReceiver = new BroadcastReceiver() {
     @Override
     public void onReceive(Context context, Intent intent) {
@@ -392,6 +402,7 @@ public class IncomingCallService extends Service {
           params.putString("callUUID", uuid);
           params.putString("endAction", Constants.ACTION_REJECTED_CALL);
           FullScreenNotificationIncomingCallModule.sendEventToJs(Constants.RNNotificationEndCallAction, params);
+          sendAppBroadcast(Constants.ACTION_INCOMING_CALL_DECLINED, bundleData);
           stopForeground(true);
         }
       }
diff --git a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/NotificationReceiverHandler.java b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/NotificationReceiverHandler.java
index beb50db..6689104 100644
--- a/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/NotificationReceiverHandler.java
+++ b/node_modules/react-native-full-screen-notification-incoming-call/android/src/main/java/com/reactnativefullscreennotificationincomingcall/NotificationReceiverHandler.java
@@ -2,6 +2,7 @@ package com.reactnativefullscreennotificationincomingcall;
 
 
 import android.annotation.SuppressLint;
+import android.content.ComponentName;
 import android.content.Context;
 import android.content.Intent;
 import android.os.Build;
@@ -9,11 +10,17 @@ import android.os.Bundle;
 import android.util.Log;
 import com.facebook.react.bridge.Arguments;
 import com.facebook.react.bridge.WritableMap;
+import org.json.JSONObject;
 
 public class NotificationReceiverHandler {
   private static final String TAG = "NotificationReceiverHandler";
   private static boolean canClick = true;
   private static boolean openedInComing = true;
+
+  // Must match sooprsVendor PendingCallActionStorage
+  private static final String PENDING_PREFS = "vendor_pending_call_action";
+  private static final String PENDING_KEY = "pending_action_json";
+
   static void handleNotification(Context context, Intent intent) {
     handleNotificationIntent(context, intent);
   }
@@ -63,19 +70,108 @@ public class NotificationReceiverHandler {
       Intent it = new Intent(Intent.ACTION_CLOSE_SYSTEM_DIALOGS);
       context.sendBroadcast(it);
     }
+
+    String payload = bundle != null && bundle.containsKey("payload")
+      ? bundle.getString("payload")
+      : null;
+
+    // Persist + bring main app forward BEFORE tearing down the call Activity.
+    // finishAndRemoveTask() must not race ahead of MainActivity launch.
+    savePendingAcceptCall(context, payload, uuid);
+    stopIncomingRingtone(context);
+    launchMainApp(context);
+
     if (IncomingCallActivity.active) {
       IncomingCallActivity.getInstance().destroyActivity(false);
     }
+
     WritableMap params = Arguments.createMap();
-    if(bundle.containsKey("payload")){
+    if(bundle != null && bundle.containsKey("payload")){
       params.putString("payload",bundle.getString("payload"));
     }
     params.putBoolean("accept", true);
     params.putString("callUUID", uuid);
+    sendAppBroadcast(context, Constants.ACTION_INCOMING_CALL_ANSWERED, bundle);
     FullScreenNotificationIncomingCallModule.sendEventToJs(eventName, params);
     context.stopService(new Intent(context, IncomingCallService.class));
   }
 
+  static void savePendingRejectCall(Context context, String payloadJson, String callUUID) {
+    try {
+      JSONObject data = payloadJson != null && !payloadJson.isEmpty()
+        ? new JSONObject(payloadJson)
+        : new JSONObject();
+      if (!data.has("callUUID")) {
+        data.put("callUUID", callUUID);
+      }
+      JSONObject action = new JSONObject();
+      action.put("action", "reject");
+      action.put("savedAt", System.currentTimeMillis());
+      action.put("data", data);
+      context.getSharedPreferences(PENDING_PREFS, Context.MODE_PRIVATE)
+        .edit()
+        .putString(PENDING_KEY, action.toString())
+        .commit();
+    } catch (Exception e) {
+      Log.e(TAG, "Failed to save pending reject call", e);
+    }
+  }
+
+  private static void savePendingAcceptCall(Context context, String payloadJson, String callUUID) {
+    try {
+      JSONObject data = payloadJson != null && !payloadJson.isEmpty()
+        ? new JSONObject(payloadJson)
+        : new JSONObject();
+      if (!data.has("callUUID")) {
+        data.put("callUUID", callUUID);
+      }
+      JSONObject action = new JSONObject();
+      action.put("action", "accept");
+      action.put("savedAt", System.currentTimeMillis());
+      action.put("data", data);
+      context.getSharedPreferences(PENDING_PREFS, Context.MODE_PRIVATE)
+        .edit()
+        .putString(PENDING_KEY, action.toString())
+        .commit();
+    } catch (Exception e) {
+      Log.e(TAG, "Failed to save pending accept call", e);
+    }
+  }
+
+  private static void launchMainApp(Context context) {
+    try {
+      Class<?> helper = Class.forName("com.sooprsvendor.call.IncomingCallLaunchHelper");
+      java.lang.reflect.Method method = helper.getMethod("launchMainApp", Context.class);
+      method.invoke(null, context.getApplicationContext());
+    } catch (Exception e) {
+      Log.e(TAG, "launchMainApp via IncomingCallLaunchHelper failed", e);
+      Intent focusIntent = context.getPackageManager().getLaunchIntentForPackage(context.getPackageName());
+      if (focusIntent == null) {
+        return;
+      }
+      focusIntent.addFlags(
+        Intent.FLAG_ACTIVITY_NEW_TASK |
+        Intent.FLAG_ACTIVITY_SINGLE_TOP |
+        Intent.FLAG_ACTIVITY_REORDER_TO_FRONT
+      );
+      focusIntent.putExtra("from_incoming_call", true);
+      context.getApplicationContext().startActivity(focusIntent);
+    }
+  }
+
+  static void stopIncomingRingtone(Context context) {
+    try {
+      Intent stopRingtone = new Intent();
+      stopRingtone.setComponent(new ComponentName(
+        context.getPackageName(),
+        "com.sooprsvendor.call.IncomingCallRingtoneService"
+      ));
+      context.stopService(stopRingtone);
+    } catch (Exception e) {
+      Log.w(TAG, "Failed to stop incoming ringtone", e);
+    }
+  }
+
   @SuppressLint("LongLogTag")
   private static void handleNotificationPressIntent(Context context, Intent intent) {
     Intent newIntent = new Intent(context, IncomingCallActivity.class);
@@ -87,4 +183,13 @@ public class NotificationReceiverHandler {
     newIntent.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
     context.startActivity(newIntent);
   }
+
+  private static void sendAppBroadcast(Context context, String action, Bundle bundle) {
+    Intent intent = new Intent(action);
+    intent.setPackage(context.getPackageName());
+    if (bundle != null) {
+      intent.putExtras(bundle);
+    }
+    context.sendBroadcast(intent);
+  }
 }
